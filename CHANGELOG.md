# 更新日志

## 2026-04-16

### 修复

- **start.sh**：将 Node.js 版本检查与 Vite 8 的 `engines` 要求对齐（`^20.19.0 || >=22.12.0`）。此前仅校验主版本 ≥ 18，在 Agent Shell 等未加载 nvm、使用系统旧版 Node（如 20.11.0）时，脚本会先 `kill` 掉端口上已在运行的服务，再因 Vite 启动失败导致页面断连；现会在版本不符时直接退出，不再误杀已有进程。

### 改进

- **start.sh**：当 6001 端口已被占用时，若判定为当前项目目录下的 Vite 开发服务，则不再强制终止进程，提示可直接使用已有服务并正常退出，避免重复启动时的不必要中断。
