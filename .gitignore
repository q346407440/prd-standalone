# Dependencies
node_modules/

# Build output
dist/
dist-ssr/

# Vite 根目录本地缓存（依赖预构建等，不应入库）
.vite/

# Environment secrets (use .env.example as template)
*.local
.env
.env.*
!.env.example

# Local runtime data (auth tokens, sync snapshots, image cache)
.local/

# PRD 业务内容（每个人的文档和截图不同，不提交）
pages/doc-*/
pages/.active-doc.json
# 本地自动备份镜像（pages/<slug>/ → pages-backup/<slug>/）
pages-backup/
public/prd/*.png
public/prd/annotations/

# Logs
logs/
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

# Bugfix 記錄（僅本地參考，不提交）
bugfix/

# OS
.DS_Store
Thumbs.db

# 本地临时截图（调试用，不入库）
tmp-chrome-*.png

# Editor
.vscode/*
!.vscode/extensions.json
.idea/
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?
