---
description: PRD 设计列截图：优先 Chrome DevTools MCP、启用检测、67% 成图法与禁止项
globs: pages/**/*.md
alwaysApply: false
---

# PRD 设计列截图（`设计/原型稿` 列）

> 完整流程与工具名以 `.cursor/skills/prd-spec/SKILL.md` **「七、设计列截图」** 为准；本规则为 Agent 必须遵守的**摘要约束**。

## 截图入库位置（强制 · colocated）

- **新增 PRD 截图**一律落到当前激活文档的 **`pages/<slug>/assets/<file>.png`**（slug 从 `pages/.active-doc.json` 读取），正文里写 **`![语义说明](./assets/<file>.png)`** 相对路径——这样图随文档走，Cursor 内可点跳转、与前端渲染 / 离线包 / 原生 MD 导出 / 飞书同步全链路一致。
- **禁止**新写 `/prd/` 绝对路径或把图落到 `public/prd/`。兼容期 `public/prd/*.png` 仅留旧引用，不再产生新文件。
- **核对 PNG 时**：`pages/doc-*/` 与 `public/prd/*.png` 都被 `.gitignore` 整目录忽略，**Glob / 索引**可能搜不到；**勿**据此断言「没有图」。核对用只读 **`ls pages/<slug>/assets/`** / **`test -f`** 或 **Read** 绝对路径；详见 **prd-spec「七」** 与 **`prd-writing-guard.mdc`** 同主题条。

## 工具优先级（强制）

1. **先执行启用检测**：在打开本机 Demo、查看页面、产出**设计列入库 PNG** 之前，检查 Cursor 是否已配置并启用 **Chrome DevTools MCP**（MCP server 名常见为 **`user-chrome-devtools`**，或以 Settings → Tools & MCP 列表为准；亦可查看 `mcps/user-chrome-devtools/tools/` 等目录下是否存在 `navigate_page`、`take_screenshot`、`evaluate_script` 等工具描述）。
2. **若已配置且可用**：**固定使用 Chrome DevTools MCP** 完成导航、快照、**视口截图**、`evaluate_script` 等；**不得**将 **`cursor-ide-browser`** 或其它浏览器 MCP 作为首选。
3. **仅当**未配置、未启用、连接失败，或用户当轮**明确要求**仅用某一浏览器工具时，再使用备选路径（如 Cursor 内置 Browser），且仍须遵守下文 **67%** 与 **视口 PNG + 按比例裁切** 规则。

## Chrome DevTools MCP「连不上」：profile 占用

> 典型报错形如：`The browser is already running for .../chrome-devtools-mcp/chrome-profile`。**不是**网络故障，而是 **同一 user-data-dir 已被 Chrome 占用**，MCP 无法再启第二实例。

**处理顺序（Agent 可照做）**

1. **先关** MCP 自动打开的那类 Chrome 窗口（若仍可见）。
2. **结束占用该 profile 的 Chrome 进程**（只杀带 MCP profile 的实例，**不要** `killall Google Chrome` 误杀日常浏览器）：
   - macOS 示例：`pkill -f "user-data-dir=.*/chrome-devtools-mcp/chrome-profile"`
   - 或：`ps aux | grep chrome-devtools-mcp/chrome-profile`，对**主进程** `kill <PID>`（子进程通常会随主进程退出）。
3. **重试** MCP：`list_pages`、`navigate_page` 等应恢复正常。
4. **仍失败**：再考虑备选 **cursor-ide-browser**（若当前环境已启用），或 PRD 设计列 **截图待补**（可在对话或 README 中说明）；**仍禁止**为截图安装 Playwright（见下节）。

## 禁止为截图安装额外自动化栈

- **不要**为产出 PRD 设计列截图而 **`npm install` / 引入 Playwright、Puppeteer、Selenium** 等无头浏览器依赖；**不要**在业务仓或 `prd-standalone` 内新增上述依赖「只为截一张图」。
- 成图路径**仅限**：**Chrome DevTools MCP**（及 SKILL 允许的备选 **cursor-ide-browser**）+ 本机 **`sips` / Pillow 等裁切**（无头浏览器栈以外的像素处理可接受）。
- 若 MCP 暂时不可用：**宁可**在设计列注明 **截图待补**（对话或 README 说明如何补），也**不要**用 Playwright 等绕过。

## 页缩放（强制 67%）

- **设计列入库截图**必须在浏览器 **页缩放 67%** 下完成（与配置端常见观感对齐）。
- **做法**：`Meta+0` / `Control+0` 复位后，连续 **4 次** `Meta+-` / `Control+-`，直至地址栏显示 **67%**。
- **缩放复用**：同一 Tab 内**首张图**完成缩放后，**后续图**不再重复 `Meta+0` + 4× `Meta+-`——Chrome 页缩放是 **Tab 级持久**，不刷新则不变。仅在 **navigate_page（整页跳转）** 或 **手动刷新** 后重做缩放。
- **禁止**：为入库 PNG 使用 **100%** 或任意非 **67%** 页缩放；**禁止**使用 `document.documentElement.style.zoom`（或等价）与页缩放混用。

## 两阶段截图流程（强制）

截图分**探索阶段**和**入库阶段**，二者职责与工具选择不同。

### 探索阶段（发现值得截图的状态）

- 目的：理解页面结构、发现交互状态、确认哪些场景需要入 PRD。
- **允许**使用 `take_snapshot` + `click(uid)` 逐步交互——这是 Agent 理解页面的必要手段。
- 本阶段**不产出入库 PNG**，探索可在 **100%** 或任意缩放下进行（无须先 67%）。

### 入库阶段（确认目标后高效成图）

- 目的：对**已确定**的若干状态，按 67% 规范快速产出入库 PNG。
- **状态构造用 `evaluate_script` 合并**：将探索阶段试过的多步交互（切 Tab、展开折叠卡、点击按钮、填值、失焦等）**合并到一次 `evaluate_script`** 中完成，**不再重复** `take_snapshot` → `click(uid)` → `take_snapshot` 的逐步链路。脚本末尾 `scrollIntoView` 目标容器并返回 `getBoundingClientRect()` + `innerWidth`/`innerHeight`。
- **`evaluate_script` 内用 DOM API**（`querySelector`、`.click()`、原生 input setter + `dispatchEvent`、`.blur()`）构造状态；React 更新需 `await new Promise(r => setTimeout(r, 60~100))`。
- **同模块多张图**：在入库阶段内，每张图为 **一次 `evaluate_script`（构造+rect）+ 一次 `take_screenshot`（无 uid）+ 一次 Python 裁切**，三步一图。

## 标准成图方法（模块 / 局部）

- **禁止**使用 DevTools **`take_screenshot(uid)`** 作为设计列入库成图（67% 下节点盒映射易错）。
- **必须**采用：**整视口** `take_screenshot`（**不传 uid**）得到 PNG，再用目标元素的 **`getBoundingClientRect()`**（与视口同一时刻、同为 CSS 像素）按  
  **`裁切像素 = rect × (PNG 像素宽高 ÷ window.innerWidth / innerHeight)`**  
  在本机裁切后写入 **`pages/<slug>/assets/xxx.png`**（slug 取当前激活文档）。
- **浮层 / 下拉**：若目标与主容器分离，对**相关多块** rect **求并集**后再裁切，可略加 padding 保留阴影。
- **成图前**可对目标 `scrollIntoView`；需隐藏 Demo-only 控件时可用 `evaluate_script` 注入 `<style>`，**不得**改业务源码。

## 设计列单元格允许内容

- **以**：`![语义说明](./assets/xxx.png)`（`[]` **须非空**，见 `prd-writing-guard` / **prd-spec·五 → 图片 Markdown alt**）**为主**。
- **可选**：极短场景或状态一句（勿与 alt、`交互` 列重复）；若用标题，`**场景预览：**` 与 `**状态说明：**` **择一即可**，非必填。
- **禁止**：流程说明、注意事项、可点击页面 URL（PRD 设计列不要求线上/本地跳转链接）。
- 格内用**原生 Markdown**（段落间空行分隔），**废除** `<br>` 换行。模板见 SKILL **「七 → 设计列模板」**。

## 详细步骤编号

- 见 **prd-spec** SKILL **「七 → 67% 标准成图法（两阶段）」**：探索阶段步骤 1～5、入库阶段步骤 A（缩放）+ B（三步循环 3～5），与 **「启用检测（强制）」**。
