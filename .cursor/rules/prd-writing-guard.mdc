---
description: PRD 写回兜底保护：防止破坏正文结构
alwaysApply: true
---

# PRD Writing Guard

- PRD 正文位于 `pages/<slug>/<文档名>.md`（多文档架构，每个文档一个 `doc-NNN` 目录，目录内 `.md` 文件名即文档名），写回时必须保留 `<!-- block:type -->` 标记与图片路径格式。
- 当前激活文档记录在 `pages/.active-doc.json`，slug 为 `doc-NNN` 格式。
- 每个文档目录下包含三个关联文件：`<文档名>.md`（正文）、`<文档名>.meta.json`（展示元数据）、`<文档名>.annotations.json`（标注数据），文件名前缀始终一致。
- 默认只改 `交互` / `逻辑` 列，不改 `设计/原型稿` 列，除非用户明确要求。
- 最小写回单位是单个表格行，不改目标行以外的 block。
- **禁止以命令行改任意 `.md`（强制）**：不得通过 Shell / 终端对 Markdown 做**内容替换、追加、删除行或整段重写**（包括但不限于 `sed` / `awk` / `perl -pi` / `python` 等脚本读写字节）。**原因**：重复标记（如 `<!-- cell:r1:交互 -->`）易导致「命中第一处」类事故；须用编辑器侧工具（如 **apply_patch**、或带**唯一章节锚点**的整块替换），且替换串须含只出现一次的前后文。**允许**：唯读检查（如 `grep` / `rg` 定位行号、`wc -l`、只读 `cat`/`head` 核对），不修改文件内容。
- 完整协议见 `.cursor/skills/prd-spec/SKILL.md`。
- **交互 / 逻辑列换行（强制）**：多字段、多规则模块须按「**加粗小节** → 一级 `-` → 必要处 `  - `」展开，**禁止**单条 bullet 内堆长句、**逻辑** 列用分号串多条规则；详见 SKILL **「〇·一」第 4 条**与 **「五 → …换行与子项列举（强制）」**。
- **同一 `block:paragraph` 内多行正文**：仍属同一段落、需手动换行时，直接换行即可（解析器自动收入同一 block）；**行间不留空行**（空行会拆成多个 paragraph block）。详见 SKILL **「〇·一」第 12 条**、**「六 → `paragraph`：同一段落内多行」**。
- **hover / 说明气泡定稿**：问号、帮助图标等触发的说明句在 **交互** 列须写 **`- **【hover 文案】**：`** + 全文（全角冒号在闭合 `**` 之后，兼容编辑器 markdown-it），**禁止** **说明**（…）括号形式；详见 SKILL **「〇·一」第 3 条**、**「五 → hover / 说明气泡定稿」**。
- **逻辑列**：无页后业务规则时 **整格留空**；**禁止**用实现向套话撑满 **逻辑** 列（详见 SKILL **「五 → `逻辑` 列 / 逻辑列禁写」**）。
- **设计列截图**（工具优先级、67% 成图法、禁止项）见 `.cursor/rules/prd-screenshot.mdc`（与 SKILL **「七」** 一致）。
- **图片 Markdown alt（强制）**：正文与表格格内凡引用 `public/prd/` 图片，须写 **`![语义说明](/prd/xxx.png)`**，**禁止**空 alt 的 `![](/prd/xxx.png)`。`[]` 内用**简短可读中文**说明画面内容（模块 / 界面区域 / 关键状态），便于检索与 Agent 理解「这张图是什么」。细则见 `.cursor/skills/prd-spec/SKILL.md` **「五 → 图片 Markdown alt」**。
- **表格格内图片与列表（强制）**：凡 `/prd/` 图须**单独成行**，行首直接 **`![语义说明](/prd/xxx.png)`**。**禁止**写成有序/无序列表项后再跟一张图，例如 `    - ![](/prd/xxx.png)`、`1. ![](/prd/xxx.png)`（飞书不支持该结构；本仓库产品侧亦以**独立图片块**展示，不在前端用列表项语义套图）。需要「先列表、后附图」时：列表与图之间用**空行**分段，图行仍**不带** `-` / `数字.` 前缀。细则见 **prd-spec·五 → 格内图片与列表**。
- **`public/prd/*.png` 与 Git（Agent 核对文件时）**：本仓库 **`.gitignore` 忽略 `public/prd/*.png`**（各人截图通常不入库）。部分 **Glob / 工作区索引** 会因此**列不出**这些文件。**不得**仅凭「搜索无结果」断言「没有图」。核对磁盘是否已有某 PNG：用 **只读** 终端 `ls public/prd/`、`test -f public/prd/xxx.png`，或对**已知绝对路径**用编辑器 **Read**；PRD 前端能显示 `/prd/xxx.png` 即一般表示 **`public/prd/xxx.png` 本机存在**（Vite 从 `public/` 原样提供）。细则见 `.cursor/skills/prd-spec/SKILL.md` **「零」**、**「七 → public/prd/ 与 Git」**。

## PRD block 语法约定

PRD 正文 `.md` 中每个内容节点前必须有对应的 `<!-- block:type -->` 标记，共 8 种，缺一不可：

| block 标记 | 紧跟的 Markdown 内容 | 用途 |
|---|---|---|
| `<!-- block:h1 -->` | `# 标题` | 文档大标题；**允许多个** `block:h1` 分段（如文档名 + `# 需求概述`），顺序由文件自上而下决定 |
| `<!-- block:h2 -->` | `## 标题` | 一级功能大块（如「弹窗公告列表」「会员弹窗」） |
| `<!-- block:h3 -->` | `### 标题` | 功能模块小节（如「固定受众」「营销折扣说明」） |
| `<!-- block:h4 -->` | `#### 标题` | h3 下的子模块（如某配置区块下的多个子配置项） |
| `<!-- block:table -->` | **cell 标记格式**（见下文） | 需求表、索引表、功能清单等；**列数与表头按本节需要**，不限于三列 |
| `<!-- block:paragraph -->` | 普通段落文字 | 说明文、列表、纯规则等；**可与同标题下的 `table` / `mermaid` 等交替出现** |
| `<!-- block:mermaid -->` | `` ```mermaid`` 围栏代码块 | Mermaid 流程图/时序图等可视化图表 |
| `<!-- block:mindmap -->` | Markdown 缩进列表（`- 根节点\n  - 子节点`） | 思维导图可视化，**必须**用 `- ` 列表格式 |

**使用规则：**
- **章节层级**用 `<!-- block:h1 -->` … `<!-- block:h4 -->` 对应 `#` … `####`；**表格**用 `<!-- block:table -->`；**常规正文**用 `<!-- block:paragraph -->`；**流程/时序图**用 `<!-- block:mermaid -->`；**思维导图**用 `<!-- block:mindmap -->`。
- 同一 `h3` / `h4` 下**可连续多个** `block`（例如 `paragraph` → `table` → `paragraph` → `mermaid`），按需编排即可。
- 每个标记独占一行，标记与其跟随的内容之间空一行。
- 顶层 `<!-- block:mermaid -->` 使用 `` ```mermaid `` 围栏。

## 表格 cell 标记格式（v2）

表格不再使用 GFM 管道行，改为 cell 标记格式。`block:table` 标记行携带 `cols` 属性（**无 id**），每个 cell 标记自带行号，格内用原生 Markdown。

**结构示例：**

```
<!-- block:table cols="设计/原型稿,交互,逻辑" -->
<!-- cell:r1:设计/原型稿 -->
![模块名-界面区域-状态或要点](/prd/xxx.png)

（可选：一两句场景或状态；勿与图片 alt 重复。）
<!-- cell:r1:交互 -->
**模块概述**

- 第一条
- 第二条
  - 子项
<!-- cell:r1:逻辑 -->

<!-- cell:r2:设计/原型稿 -->
![另一模块-配置面板预览](/prd/yyy.png)
<!-- cell:r2:交互 -->
第二行交互内容
<!-- cell:r2:逻辑 -->
```

**规则：**
- `<!-- block:table cols="列1,列2,列3" -->` — 表头列名以逗号分隔写在 `cols` 属性中；**无 id 属性**。
- `<!-- cell:rN:列名 -->` — 格标记，`rN` 为行号（从 1 递增），列名须与 `cols` 中一致。**无独立 `<!-- row:N -->` 标记**。
- Agent 可用 `grep "cell:r2:交互"` 精准定位第 2 行交互列。
- 格内使用**原生 Markdown**：段落间空行分隔、列表直接写 `- ` / `  - `、图片 **`![语义说明](url)`**（`[]` 非空）**单独一行且行首即为 `![`**（**禁止** `  - ![…](…)` / `1. ![…](…)` 等列表前缀挂图）、Mermaid 用标准 `` ```mermaid `` 围栏。
- **废除** GFM 表格中的 `<br>` 换行、`:::mermaid:::` / `:::mindmap:::` 包裹语法。
- 格内空格留空即可（`<!-- cell:r1:逻辑 -->` 后无内容）。
- Parser 仍兼容读取旧 GFM 管道行格式（打开→保存即自动迁移为新格式）。
