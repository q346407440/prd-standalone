---
description: PRD 写回兜底保护：防止破坏正文结构
alwaysApply: true
---

# PRD Writing Guard

- PRD 正文位于 `pages/<slug>/<文档名>.md`（多文档架构，每个文档一个 `doc-NNN` 目录，目录内 `.md` 文件名即文档名），写回时必须保留 `<!-- block:type -->` 标记、Markdown 表格结构与图片路径格式。
- 当前激活文档记录在 `pages/.active-doc.json`，slug 为 `doc-NNN` 格式。
- 每个文档目录下包含三个关联文件：`<文档名>.md`（正文）、`<文档名>.meta.json`（展示元数据）、`<文档名>.annotations.json`（标注数据），文件名前缀始终一致。
- 默认只改 `交互` / `逻辑` 列，不改 `设计/原型稿` 列，除非用户明确要求。
- 最小写回单位是单个表格行，不改目标行以外的 block。
- **GFM 表格分隔行**：表头下一行用最短合法写法（如三列 `| --- | --- | --- |`），禁止为与表头「视觉对齐」而拉长 `-`。详见 `.cursor/skills/prd-agent/SKILL.md` **「五 → PRD 正文中的 GFM 表格（表格外层）」**。
- 完整协议见 `.cursor/skills/prd-agent/SKILL.md`。
- **交互 / 逻辑列换行（强制）**：多字段、多规则模块须按「**加粗小节** → 一级 `-` → 必要处 `  - `」展开，**禁止**单条 bullet 内堆长句、**逻辑** 列用分号串多条规则；详见 SKILL **「〇·一」第 4 条**与 **「五 → …换行与子项列举（强制）」**。表格单元格内 **一般叙述**仍用 `<br>`（勿随意用真实换行拆 GFM 表行）。
- **同一 `block:paragraph` 内多行正文**：仍属同一段落、需手动换行时，直接换行即可（解析器自动收入同一 block）；**行间不留空行**（空行会拆成多个 paragraph block）。勿与表格单元格 `<br>` 混用。详见 SKILL **「〇·一」第 13 条**、**「六 → `paragraph`：同一段落内多行」**。
- **表格单元格内的 Mermaid / Mindmap（与顶层 block 不同）**：GFM 表格单元格内 **禁止**使用 `` ```mermaid `` / `` ``` `` 标准围栏嵌入多行图代码；须用 **`:::mermaid:::` … `:::end-mermaid:::`**（Mindmap 为 **`:::mindmap:::` … `:::end-mindmap:::`**）包裹，解析与续行规则（首行仍以 `|` 起表行、图代码续行不以 `|` 开头直至闭合标签等）见 SKILL **「六 → Mermaid 图表 block」「六 → 思维导图 block」** 中「表格单元格内」。
- **表格单元格图片**：禁止行内图文混排；`![](url)` 须独立成段（`<br>` 与前后文分隔），与飞书同步及解析规范化一致；细则见 SKILL **「五 → 交互列 Markdown 写法规范」** 中「表格图片」。
- **hover / 说明气泡定稿**：问号、帮助图标等触发的说明句在 **交互** 列须写 **`- **【hover 文案】**：`** + 全文（全角冒号在闭合 `**` 之后，兼容编辑器 markdown-it），**禁止** **说明**（…）括号形式；详见 SKILL **「〇·一」第 3 条**、**「五 → hover / 说明气泡定稿」**。
- **逻辑列**：无页后业务规则时 **整格留空**；**禁止**用实现向套话撑满 **逻辑** 列（详见 SKILL **「五 → `逻辑` 列 / 逻辑列禁写」**）。
- **设计列截图**（工具优先级、67% 成图法、禁止项）见 `.cursor/rules/prd-screenshot.mdc`（与 SKILL **「七」** 一致）。

## PRD block 语法约定

PRD 正文 `.md` 中每个内容节点前必须有对应的 `<!-- block:type -->` 标记，共 8 种，缺一不可：

| block 标记 | 紧跟的 Markdown 内容 | 用途 |
|---|---|---|
| `<!-- block:h1 -->` | `# 标题` | 文档大标题；**允许多个** `block:h1` 分段（如文档名 + `# 需求概述`），顺序由文件自上而下决定 |
| `<!-- block:h2 -->` | `## 标题` | 一级功能大块（如「弹窗公告列表」「会员弹窗」） |
| `<!-- block:h3 -->` | `### 标题` | 功能模块小节（如「固定受众」「营销折扣说明」） |
| `<!-- block:h4 -->` | `#### 标题` | h3 下的子模块（如某配置区块下的多个子配置项） |
| `<!-- block:table -->` | GFM 表格（常见为三列 **设计/原型稿 \| 交互 \| 逻辑**） | 需求表、索引表、功能清单等；**列数与表头按本节需要**，不限于三列 |
| `<!-- block:paragraph -->` | 普通段落文字 | 说明文、列表、纯规则等；**可与同标题下的 `table` / `mermaid` 等交替出现** |
| `<!-- block:mermaid -->` | `` ```mermaid`` 围栏代码块 | Mermaid 流程图/时序图等可视化图表 |
| `<!-- block:mindmap -->` | Markdown 缩进列表（`- 根节点\n  - 子节点`） | 思维导图可视化，**必须**用 `- ` 列表格式 |

**使用规则：**
- **章节层级**用 `<!-- block:h1 -->` … `<!-- block:h4 -->` 对应 `#` … `####`；**表格**（含三列需求表、索引表、清单等）用 `<!-- block:table -->`；**常规正文**（段落、列表、说明）用 `<!-- block:paragraph -->`；**流程/时序图**用 `<!-- block:mermaid -->`；**思维导图**用 `<!-- block:mindmap -->`。
- 同一 `h3` / `h4` 下**可连续多个** `block`（例如 `paragraph` → `table` → `paragraph` → `mermaid`），按需编排即可。
- 每个标记独占一行，标记与其跟随的内容之间空一行。
- 顶层 `<!-- block:mermaid -->` 使用 `` ```mermaid `` 围栏；**表格单元格内**的 Mermaid / Mindmap **不得**用围栏，须用 **`:::…:::`** 语法，见上文 **「表格单元格内的 Mermaid / Mindmap」**。
