---
description: PRD 写回兜底保护：防止破坏正文结构
alwaysApply: true
---

# PRD Writing Guard

## 动笔前约束（了解全貌再动笔 · 强制）

Agent **修改或新增 PRD 正文之前**，必须满足以下三条；细则见 SKILL **「三 → 执行流程」**。

1. **读全范围**：用户指向某标题（h2/h3/h4）或行号时，**必须**读到该标题到下一个同级标题之间的**所有内容**（含子标题、table 行、paragraph 等）。动笔前先输出一句结构摘要（如「该 h3 下有 2 个 h4、3 个 table 共 8 行、1 个 paragraph」），**不得**只读局部 30 行就开始写。
2. **核实真实状态**：修改涉及 UI 描述（交互列或设计列）时，**必须**先确认真实 UI 状态——按以下优先级：**(a)** 用户当轮提供的截图（已有则先消化，可替代跑前端，但应声明「基于你提供的截图」）；**(b)** Chrome DevTools MCP（本地 Demo 在跑时，优先于 cursor-ide-browser）；**(c)** cursor-ide-browser（DevTools 不可用时备选）。**不得**在未见真实 UI 的情况下凭空描述交互。
3. **对比判断再写**：动笔前须能回答「现有 PRD 哪些内容与真实状态一致（保留）、哪些过时或缺失（改/补）、哪些多余（删）」。**模块级修改**（范围为完整 h3/h4 或更大章节）时，**三列都要判断是否需要变更**（设计列截图是否仍准确、交互列是否与 UI 一致、逻辑列是否需补充或清空）——判断是必须的，修改是按需的。单行/单格小修仅改用户指定范围。

**按需：代码侦察**——修改涉及**逻辑列**或用户要求「写全模块」时，应在业务代码中搜索目标组件/路由，查条件分支、注释、枚举、接口处理，补充前端不可见的页后逻辑。

## 文件结构与写回范围

- PRD 正文位于 `pages/<slug>/<文档名>.md`（多文档架构，每个文档一个 `doc-NNN` 目录，目录内 `.md` 文件名即文档名），写回时必须保留 `<!-- block:type -->` 标记与图片路径格式。
- 当前激活文档记录在 `pages/.active-doc.json`，slug 为 `doc-NNN` 格式。
- 每个文档目录下包含三个关联文件：`<文档名>.md`（正文）、`<文档名>.meta.json`（展示元数据）、`<文档名>.annotations.json`（标注数据），文件名前缀始终一致。
- 默认只改 `交互` / `逻辑` 列，不改 `设计/原型稿` 列，除非用户明确要求。**例外**：模块级修改时三列均须**判断**是否需变更（见上方第 3 条），但仍只**修改**确实需要改的列。
- 最小写回单位是单个表格行，不改目标行以外的 block。
- **禁止以命令行改任意 `.md`（强制）**：不得通过 Shell / 终端对 Markdown 做**内容替换、追加、删除行或整段重写**（包括但不限于 `sed` / `awk` / `perl -pi` / `python` 等脚本读写字节）。须用编辑器侧工具（如 **apply_patch**、或带**唯一章节锚点**的整块替换），且替换串须含只出现一次的前后文。**允许**：唯读检查（如 `grep` / `rg` 定位行号、`wc -l`），不修改文件内容。

## 正文禁入内容（强制）

PRD 正文只承载**产品定义**（行为、规则、界面状态）。以下内容**禁止**写入任何列：

1. **Agent / 工具工作流程**：截图怎么裁、缩放比例多少、对话框操作步骤等——属 Agent 规范，不是产品需求。
2. **工程坐标**：项目路径、路由、快照壳名称、Demo URL（如 `/member-offer/2026-04-15/editor-shell`）。PRD 不绑定某次构建或某个分支。
3. **版本对齐声明**：「与 xxx 快照壳一致」「以对应版本实装为准」「本节配图与行为以 xxx 路由实装为准」——PRD 描述的是**要交付的行为**，不是与某环境对齐的免责声明。
4. **本地存储 / 内部 state / mock 标记 / 代码标识符**：localStorage key、state 字段枚举、「刷新可复现，非线上接口」等运行态说明——属开发联调信息，不入需求。行为描述中也**禁止夹带 Demo 源码的变量名、组件名、函数名**（如 `rhythmValuesByStrategy`）；应改用中文产品概念（如「每档独立保存的配置值」）。PRD 告诉开发**要做什么**，不是展示 Demo 怎么实现的。
5. **模块定位的工程话术**：「本模块位于 xxx 与 xxx 之间」「其它日期或主路径 Demo 若仍为旧布局…」——用截图或章节结构表达位置关系，不用工程语言描述 DOM 层级。
6. **渲染与样式实现指导**：CSS 定位策略（如「固定定位叠加层，避免被父级容器裁切」）、z-index、溢出处理等——属开发实现决策，不是产品需求。
7. **组件标准交互教科书**：下拉 click/Enter/Space 展收、Chip 含名称与移除、箭头表示可展开等 Web 平台惯例——开发者自然遵循，PRD 只定义偏离标准的行为。
8. **演示 / Demo / 本地 / 联调语境（强制）**：正文**禁止**出现绑定「非正式交付环境」的锚点用语，包括但不限于：**演示环境**、**Demo**、**本机**、**本地**（在「开发机 / 个人环境 / 本机页面」语义下）、**快照**（指工程快照壳）、**联调**、**mock**（作环境或数据来源说明时）、**刷新可复现** 等。需求范围、可见 Tab、能力边界等一律用**产品语言**写清（如「**商家后台**」「**本期**」「**编辑器内**」「**配置侧**」），**不得**用「某次演示」「本地跑的页面」来限定。**例外**：「本地」作**产品功能词**（如 **本地上传**、**本地文件**）可保留；**禁止**与演示 / Demo 语境混写成环境锚点。与第 2、3、4 条一并执行。
9. **语气：交付研发 + 口语好懂（强制）**：正文是**交给研发落地、可评审**的需求说明，**不是** Agent 笔记、**不是**与环境对齐的免责声明。表述上**宜口语化、短句、先结论后条件**，把规则说清楚，**避免**学术论文腔、**避免**堆砌系统黑话与晦涩术语；确需术语时，用**一句人话**或括号补一句可读解释。与 **prd-writing-style**「语气与可读性」一致。

## 格式规则速查

- **交互 / 逻辑列换行**：多字段、多规则模块须按「**加粗小节**（纯加粗，无 `-` 前缀）→ 一级 `-` → 必要处 `  - `」展开；**分组名 / 层级名本身禁止用 `-` 列表项**，加粗即可，子项才开始用 `-`；**禁止**单条 bullet 内堆长句、分号串多条规则。
- **hover 定稿**：交互列固定 **`- **【hover 文案】**：`** + 全文（全角冒号在闭合 `**` 后），**禁止** 说明（…）括号形式。
- **逻辑列**：无页后业务规则时 **整格留空**；**禁止**用实现向套话撑满。
- **图片 alt（强制）**：须写 **`![语义说明](/prd/xxx.png)`**，**禁止**空 alt `![](/prd/xxx.png)`。
- **格内图片**：`/prd/` 图须**单独成行**，行首即 `![`；**禁止** `- ![…](…)` / `1. ![…](…)` 等列表前缀挂图。
- **paragraph 多行**：同一段落直接换行，**行间不留空行**（空行会拆块）。
- **设计列版式**：**先图后文**——图在前，状态说明 / 场景预览等文字放在图下方；**冒号后多项必须换行**，不堆一行。见 `.cursor/rules/prd-screenshot.mdc`（与 SKILL **「七」** 一致）。
- **`public/prd/*.png` 与 Git**：`.gitignore` 忽略 `public/prd/*.png`，**Glob 搜不到 ≠ 磁盘没有**，核对用只读 `ls` / `test -f` / **Read**。

## block 与表格语法（防错摘要）

- PRD 正文每个内容节点前**必须**有 `<!-- block:type -->` 标记（共 8 种：`h1`–`h4`、`table`、`paragraph`、`mermaid`、`mindmap`）。标记独占一行，与内容间空一行。
- 表格使用 **cell 标记格式**：`<!-- block:table cols="列1,列2,..." -->`（**无 id**）+ `<!-- cell:rN:列名 -->`（N 从 1 递增）。**无独立 `<!-- row:N -->`**。格内用**原生 Markdown**（段落空行分隔、列表 `- ` / `  - `、图片独立成行、Mermaid 标准围栏）。
- **废除**：GFM 管道行表格、`<br>` 换行、`:::mermaid:::` / `:::mindmap:::` 包裹语法。Parser 兼容读取旧格式（打开→保存自动迁移）。
- 完整语法与示例见 `.cursor/skills/prd-spec/SKILL.md`（**「五」「六」** 及 `prd-writing-guard` 首节 block 约定）。
